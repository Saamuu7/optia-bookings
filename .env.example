# Copia este archivo a `.env` y reemplaza los valores con los de tu proyecto Supabase
# Vite expondrá sólo variables con prefijo VITE_

# URL pública de tu proyecto Supabase
VITE_SUPABASE_URL=https://ejemplo-ref.supabase.co

# Public (publishable) key de Supabase (pk_...)
VITE_SUPABASE_PUBLISHABLE_KEY=pk_test_ejemplo

# Ejemplo opcional: cambiar puerto por defecto de Vite (opcional)
# VITE_PORT=5173

# Nota: no subas tu `.env` a repositorios públicos si contiene claves privadas.

# Producción vs Desarrollo
# - Vite expone al cliente sólo variables con prefijo `VITE_`.
# - Si publicas la app en un host (Netlify, Vercel, VPS, etc.) debes configurar
#   estas mismas variables en el panel del host (con el mismo nombre `VITE_SUPABASE_URL`,
#   `VITE_SUPABASE_PUBLISHABLE_KEY`). No guardes claves sensibles que no deban
#   ser públicas. Para operaciones sensibles usa funciones server-side y claves
#   no expuestas al cliente (ej. Supabase SERVICE_ROLE_KEY) únicamente en servidores.

# Nota sobre la carpeta de salida de build
# - Cuando ejecutes `npm run build` la carpeta generada será `ERYCK_STYLE/`.
# - Sube el contenido de `ERYCK_STYLE/` a tu hosting cuando publiques.

